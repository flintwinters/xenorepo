name: calculator
title: Calculator
module: apps.calculator.backend.server
imports:
  - "@xenorepo/ui"
  - monotools.orchestration.management
  - monotools.runtime.application
frontend:
  artifacts:
    index:
      format: preact
      source: frontend/index.tsx
      output: index.html
  routes:
    /: index
