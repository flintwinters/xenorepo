"""Centralized application lifecycle tooling for Xenorepo.

The package owns generic discovery, build, validation, runtime, persistence,
and management contracts while monoapps retain all product-specific policy.
"""
