name: calculator
title: Calculator
module: apps.calculator.backend.server
imports:
  - "@xenorepo/lit-ui"
  - monotools.management
  - monotools.runtime
frontend:
  artifacts:
    index:
      format: lit
      source: frontend/index.ts
      output: index.html
  routes:
    /: index
