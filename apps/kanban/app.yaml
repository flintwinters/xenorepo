name: kanban
title: Kanban Console
module: apps.kanban.server
capabilities:
  - database
frontend:
  artifacts:
    index:
      format: typescript
      source: frontend/app.ts
      output: index.html
  routes:
    /: index
