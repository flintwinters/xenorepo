name: kanban
title: Kanban Console
module: apps.kanban.backend.server
capabilities:
  - database
frontend:
  artifacts:
    index:
      format: lit
      source: frontend/index.ts
      output: index.html
  routes:
    /: index
