{
  "name": "xenorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:lit": "node scripts/build-lit.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
