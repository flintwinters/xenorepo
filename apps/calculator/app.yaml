name: calculator
title: Calculation Control
module: apps.calculator.backend.server
frontend:
  artifacts:
    index:
      format: lit
      source: frontend/index.ts
      output: index.html
  routes:
    /: index
