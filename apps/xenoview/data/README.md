# Cockpit runtime data

Visible, ignored runtime databases and routinized test evidence live here. `xenoview.db` stores explicit, schema-versioned repository snapshots and may be deleted to reset local history.
