name: calendar
title: Calendar Console
module: apps.calendar.backend.server
capabilities:
  - database
frontend:
  artifacts:
    index:
      format: lit
      source: frontend/index.ts
      output: index.html
  routes:
    /: index
