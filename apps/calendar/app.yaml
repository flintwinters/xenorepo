name: calendar
title: Calendar Console
module: apps.calendar.backend.server
capabilities:
  - database
imports:
  - "@xenorepo/ui"
  - monotools.appkit
  - monotools.http
  - monotools.management
  - monotools.runtime
frontend:
  artifacts:
    index:
      format: preact
      source: frontend/index.tsx
      output: index.html
  routes:
    /: index
