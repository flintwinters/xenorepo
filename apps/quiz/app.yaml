name: quiz
title: Working Style Inventory
module: apps.quiz.backend.server
imports:
  - "@xenorepo/ui"
  - monotools.orchestration.management
  - monotools.runtime.application
frontend:
  artifacts:
    index:
      format: preact
      source: frontend/index.tsx
      output: index.html
  routes:
    /: index
