# Contact Book local data

`contacts.db` contains the durable local directory. Generated OpenAPI, build, and UI-check
artifacts also live here. These repeatable files are ignored by Git and are produced through the
root `manage.py` lifecycle; this README is the only versioned file in the directory.
